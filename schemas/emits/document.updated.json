{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "document.updated",
  "description": "An accepted save / revision event (snapshot-grained; crdt journal appends are debounced by the snapshot policy, never per-update).",
  "type": "object",
  "additionalProperties": false,
  "required": ["document_id", "workspace_id", "head_seq"],
  "properties": {
    "document_id": {"type": "string"},
    "workspace_id": {"type": "string"},
    "head_seq": {"type": "integer"}
  }
}
