{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "document.deleted",
  "description": "The document left the visible corpus — fires on trash AND on purge (consumers are idempotent, at-least-once); restore re-announces via document.created.",
  "type": "object",
  "additionalProperties": false,
  "required": ["document_id", "workspace_id"],
  "properties": {
    "document_id": {"type": "string"},
    "workspace_id": {"type": "string"}
  }
}
