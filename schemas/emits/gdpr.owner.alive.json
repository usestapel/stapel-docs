{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gdpr.owner.alive",
  "description": "Answer to gdpr.owner.probe, emitted from the same subscriber that handles gdpr.erasure.requested — so the answer proves the erasure path is consumed, not that a container is deployed. subject_types is what this owner claims.",
  "type": "object",
  "required": ["owner"],
  "properties": {
    "owner":          { "type": "string" },
    "subject_types":  { "type": "array", "items": { "type": "string" } },
    "correlation_id": { "type": "string" }
  },
  "additionalProperties": false
}
